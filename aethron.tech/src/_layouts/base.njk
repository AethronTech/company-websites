<!DOCTYPE html>
<html lang="{{ lang or 'en' }}" dir="{{ (lang or 'en') | langDir }}">
<head>
{% include "head.njk" %}
</head>
<body class="lang-{{ lang or 'en' }}">
  <header>
    {% include "navigation.njk" %}
  </header>
  
  <main>
    {{ content | safe }}
  </main>
  
  {% include "footer.njk" %}
</body>
</html>
