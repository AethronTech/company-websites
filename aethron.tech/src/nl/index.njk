---
layout: layout.njk
lang: nl
title: "Aethron Technology - Mensgerichte software. Flexibel. Betrouwbaar. Slim."
description: "Aethron Technologies versterkt IT-teams met digitale oplossingen die écht werken — van configureerbare pakketten tot maatwerk. Technologie in dienst van mensen, gedragen door beproefde methodes als Agile en ITIL."
---

<section class="hero i18n-content" lang="{{ lang or 'nl' }}">
  <div class="container">
    <div class="hero-content">
      <h1>{{ 'homepage.hero.title' | t(lang or 'nl') }}</h1>
      <p class="hero-description">
        {{ 'homepage.hero.description' | t(lang or 'nl') }}
      </p>
      <div class="hero-actions">
        <a href="{{ 'solutions/' | localizeUrl(lang or 'nl') }}" class="btn btn-primary">{{ 'homepage.hero.cta' | t(lang or 'nl') }}</a>
        <a href="{{ 'about/' | localizeUrl(lang or 'nl') }}" class="btn btn-secondary">{{ 'homepage.hero.cta_secondary' | t(lang or 'nl') }}</a>
      </div>
    </div>
  </div>
</section>

<section class="services-overview i18n-content" lang="{{ lang or 'nl' }}">
  <div class="container">
    <div class="services-grid">
      {% for service in 'homepage.services.items' | t(lang or 'nl') %}
      <div class="service-card">
        <div class="service-icon">{{ service.icon }}</div>
        <h3>{{ service.title }}</h3>
        <p>{{ service.description }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="about-summary i18n-content" lang="{{ lang or 'nl' }}">
  <div class="container">
    <div class="summary-grid">
      <div class="summary-card">
        <h3>{{ 'homepage.about_summary.title' | t(lang or 'nl') }}</h3>
        <p>{{ 'homepage.about_summary.description' | t(lang or 'nl') }}</p>
        <a href="{{ 'homepage.about_summary.link_url' | t(lang or 'nl') }}" class="btn btn-outline">{{ 'homepage.about_summary.link_text' | t(lang or 'nl') }} →</a>
      </div>

      <div class="summary-card">
        <h3>{{ 'homepage.solutions_summary.title' | t(lang or 'nl') }}</h3>
        <p>{{ 'homepage.solutions_summary.description' | t(lang or 'nl') }}</p>
        <a href="{{ 'homepage.solutions_summary.link_url' | t(lang or 'nl') }}" class="btn btn-outline">{{ 'homepage.solutions_summary.link_text' | t(lang or 'nl') }} →</a>
      </div>
    </div>
  </div>
</section>
